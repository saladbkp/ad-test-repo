{% include('header.html.twig') %}
<main>
	<h4 class="loyalty-user">#{{ user_id }}</h4>
	<h1>Loyalty points:</h1>
	<h3>{{ loyalty_points }}</h3>

	<small>All other user data is redacted for privacy</small>
</main>
{% include('footer.html.twig') %}
